{
  "name": "live-event-translator-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --use-env-proxy src/server.js",
    "dev": "node --watch --use-env-proxy src/server.js",
    "test": "node --test test/*.test.js",
    "smoke": "node --use-env-proxy scripts/smoke-realtime.mjs"
  }
}
